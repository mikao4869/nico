# 이 코드 이렇게 사용할수 있어.

      greeting.innerText="Hello"+username;

이게 

greeting.innerText=`Hello ${username}`;


이렇게 사용할 수 있다.


`string ` + `${명사}`

이렇게 사용가능!

# 왜 이렇게 써야하는데


string 이랑 변수를 하나로 합칠수 있다.


# 사용법


`${변수명}` 이렇게 사용


 `백틱` 사용