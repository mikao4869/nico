# 3.0~ 3.5 연습

코드챌링징 해야해서 매우매우 떨린상태! 열심히 예습하자

# 3.0 js를 이용해 브라우저 이용하기

html은 js와 css를 연결해주는 접착제 같은 아이이기 때문에


`html의 element들을 js를 총해 변경하고 연결할 수 있다. `


+ document: 브라우저에 존재하는 object

####  js은 html을 읽어올뿐만 아니라 html을 변경할수 도 있다.


        ex) 콘솔에서  
        
          document.title="hi"


## 3.0 요약: js는 hmlt에 연결되어있고,js는  html에서 변경가능하고 볼 수 있다..  

### js로 html이 변경가능하다. 


# 3.1 getElementById

+ getElementById:html 에서 id를 통해 element을 찾아주는것


사용법:() 안에가 꼭 `string` 이어야할것

    ex) document.getElementById("title")

이 코드에 대해서 설명은 `html에서 id가 title인것을 찾아주는 역할`을 한다. 


     ex)  document.getElementById("title);
          title.innerText="got you"

이 코드는 js에서 작성된 html처럼 보이는것

## 3.1 요약: getElementById:html에서 id가 같은것을 찾고, 그 같은 거에 대해서 쓰는거

# 3.2 querySeletor

+ getElementByClassName(): html에서 class 이름이 같은것을 찾아주는 코드

        <div class="hello">
            <h1>Grab me</h1>
        </ div>

 이러한코드에서 js에서 h1을 가져오는 방법


 + getElementByTagName(): tag 가 같은 element를 갖는거


      ex)getElementByTagName('h1');

이 코드는 `h1`인 태그를 갖고 있는 element를 찾는거

+ querySelector: 단 하나의 element를 return 하는거 
+ querySelectorAll: 여러개의 element를 return 하는거


      ex) const title=document.querySelector(".hello h1");

해석: 클래스가 hello인 h1인 element를 찾아라

#### querySelector을 사용하는데 여러개의 같은 class가 있다면? 


--> 답: 그중에 제일 첫번째인것이 나온다. 


## 3.2 요약: queryselector쓰는법: (".클래스 이름 tag");



# 3.3 event 


    function 함수(){
      event를 했을때 실행되어야 하는 함수
    }

    ex) title.addEventListner("하고싶은 event",함수 이름)

## 3.3 요약: event 사용하는 법을 배웠다. 



# 3.4