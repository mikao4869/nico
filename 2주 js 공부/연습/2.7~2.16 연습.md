# 2.7~2.16 예습


# 2.7 function


+ function의 정의: 내가 계속 반복해서 사용할 수 있는 코드 조각



+ function을 만드는 법: 

`function 이름(){} `

이런식으로 만든다. 

      ex) function sayHello(){
        여기에 적는 모든것은 sayHello가 실행될때마다 실행되는것!
      } 

+ function을 실행하는 법

`()` 을 사용한다.

function 이름() 이 function을 실행하는 방법이다. 

        sayHello();



## 2.7 강의 요약: function을 만드는 법은 function 이름(){}  /// function을 실행하는 방법은 function이름()


# 2.8 function argument 


+ function argument를 보내고 받는법 


      ex) function sayHello(name,age){  //argument를 받는법
        console.log(name);
        console.log(age);

      }

      sayHello("Nico",10);  //  이거는 argument를 보내는거 


+ Nan: 숫자가 아님
+ argument의 순서는 정말 중요하다. 

argument는 function안에서만 존재하고, 다른 곳에서 한다면 그것은 오류가 난다.!


#### object 안에서 function 하는법??

`이름과 위치가 바뀜!을 알수 있다. `
     
      ex)  const play={
        sayHello:function(name){
          consol.log("HELLO"+name)
        }
      }
      play.sayHello("lynn"); 

이 코드에 대해서 설명을 하자면

+ play.sayHello("lynn")은 console.log와 같이 비슷하게 생겼다.
`sayHello function에 "lynn" 이라는 argument를 보내고 `

sayHello function에 lynn이라는 정보가 오면 그거를 출력하는것이다. 


정보를 받는 방법은 argument로 받을수 있다. 

## 2. 8 강의 요약: argument로 function의 값을 전달할 수 있다. 