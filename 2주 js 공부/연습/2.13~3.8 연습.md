# 2.13~3.8 연습

2.13~2.15는 if문이어서 넘어간다!

# 3.6 요약: js에서 if문을 통하여 innerText 색깔 변경
같다를 표현할때는 `===` 을 사용한다. 

코드


      function handle(){
        const currentColor=h1.style.color;
        let newColor;
        if(currentColor==="bllue"){
          newColor="tomato"
        }
      }

똑같은것이 겹치면 새로운 변수를 만들고 거기에다가 집어 넣자.
+ currentColor=지금 색깔
+ newColor" 변경할 색깔 


# 3.7 전에한거를 js말고 css 에서 하기


js 코드


      function handle(){
        if(h1.className==="active"){
          h1.className="";
        }
        else{
        h1.className="active";
        }
      }

      h1.addEventListner("click",handleTitleClick);

 코드 설명: 
 + h1의 클래스네임이 active이라면: 즉 `빨간색일때는  다시 원래 색으로 변경 `
+ h1의 클래스네임이 active가 아니라면 : 즉 `파란색이라면 빨간색으로 변경`;


css 코드 

      .active{
        color="tomato";
      }


# 3.8

+ classList: class 들의 목록으로 작업할 수 있게 허용해줌

  classList는 모든것을 교체해버린다.


      if(h1.classList.contains(clickedClass))

이걸 해석하면`만약 h1의 clickedClass가 h1의 classList에 포함되어 있다면 ` 이라면 뜻이다. 

+ remove

ex) 
        h1.classList.remove(clickedClass);

+ add


ex)

        h1.classList.add(clickClass);


### toggle function

class name이 존재하는지 확인하기 위해 사용

+ if( 존재 한다. --> remove)
+ else if(존재 하지 않는다. --> add)

ex) 

      h1.classList.toggle("clicked");
  

아까의 긴 코드가 이렇게 변경된것을 확인할수 있다. 