#  6.0~6.2 예습 랜덤

랜덤 함수

      Math.floor(Math.random()*10);

하지만 우리는 뒤에 `10`이 몇개인지 모른다. 그거는 사용자가 지정해줘야하기 때문에 10 보다는 

    quotes.length로 바꿔야한다.

`length`


코드

      const todaysQuote=quotes[Math.floor(Math.random()*quotes.length)];

      quote.innerText=todaysQuote