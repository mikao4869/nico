# 5.0~5.3 연습



### interval: 매번 일어나야하는 무언가

ex) 2초마다 무언가가 실행

    function sayHello(){

      console.log("hello")'
    }

    setInterval(sayHello,5000);


+ setInterval(`function`,`몇초마다 실행 될건지 ms`)


### setTimeout(): 

    + setTimeout(sayHello,5000);



+ getDate: 날짜
+ getDay: 요일
+ getFullYear: 년
+ getHours: 시간
+ getMinutes: 분
+ getSeconds: 초


ex)   

      function getClock(){
        const date=new Date();
        clock.innerText=`${date.gerHours()}:${date.getMinutes()}`
      }

      setInterval(getClock,1000);